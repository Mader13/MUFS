{
  "name": "mufs",
  "version": "1.0.0",
  "engines": {
    "node": "16.17.1",
    "npm": "8.19.2"
  },
  "scripts": {
    "start": "cd mufs-api/public && npm start ",
    "prebuild": "cd mufs-api && npm run build",
    "build": "cd mufs-ui && npm run start"
  }
}
