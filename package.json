{
  "name": "mufs",
  "version": "1.0.0",
  "scripts": {
    "start": "cd mufs-api/built && node server.js",
    "prebuild": "cd mufs-api && npm run build",
    "build": "cd mufs-ui && npm run build"
  },
  "dependencies": {
    "bootstrap": "^5.2.2",
    "kill-port": "^2.0.1",
    "npx": "^10.2.2"
  }
}
